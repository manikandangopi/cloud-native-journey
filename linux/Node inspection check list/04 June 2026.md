# Node Inspection Checklist - 04 June 2026

## Objective

Performed a complete Linux node health inspection to verify operating system details, kernel version, network configuration, time synchronization, SSH service availability, storage utilization, memory usage, and overall system health.

---

# 1. Kernel Version Verification

### Command

```bash
uname -r
```

### Output

```text
6.8.0-124-generic
```

### Explanation

* Displays the currently running Linux kernel version.
* Kernel is the core component of the operating system.
* Used to verify compatibility with Docker, Kubernetes, and drivers.

### Observation

Current Kernel:

```text
6.8.0-124-generic
```

---

# 2. Operating System Verification

### Command

```bash
cat /etc/os-release
```

### Output

```text
Ubuntu 24.04.4 LTS (Noble Numbat)
```

### Explanation

* Displays Linux distribution details.
* Useful for package management and OS compatibility checks.

### Observation

Operating System:

```text
Ubuntu 24.04.4 LTS
```

Distribution Family:

```text
Debian Based
```

---

# 3. Host Information Verification

### Command

```bash
hostnamectl
```

### Purpose

Verify:

* Hostname
* Operating System
* Kernel
* Virtualization Platform
* Architecture

### Observation

Hostname:

```text
dev-k8s-ubuntulinux-01
```

Virtualization:

```text
Oracle VirtualBox
```

Architecture:

```text
x86_64
```

Operating System:

```text
Ubuntu 24.04.4 LTS
```

---

# 4. Network Interface Inspection

### Command

```bash
ip a
```

### Purpose

Display all network interfaces and IP addresses.

### Observation

#### Loopback Interface

```text
lo
127.0.0.1
```

Used for internal system communication.

---

#### Primary NAT Adapter

```text
enp0s3
10.0.2.15
```

Provides internet connectivity through VirtualBox NAT.

---

#### Host-Only Adapter

```text
enp0s8
192.168.56.11
```

Used for communication between host and virtual machine.

---

#### Docker Bridge Network

```text
docker0
172.17.0.1
```

Created automatically by Docker.

Used for container networking.

---

# 5. Routing Table Verification

### Command

```bash
ip route
```

### Purpose

Displays routing information used by Linux networking stack.

### Observation

Default Gateway:

```text
10.0.2.2
```

Default Interface:

```text
enp0s3
```

Host-only Network:

```text
192.168.56.0/24
```

Docker Network:

```text
172.17.0.0/16
```

---

# 6. Time Synchronization Verification

### Command

```bash
timedatectl
```

### Purpose

Verify system time and NTP synchronization.

### Observation

Time Zone:

```text
Asia/Kolkata
```

NTP Service:

```text
Active
```

System Clock Synchronization:

```text
Yes
```

### Result

Time synchronization is working correctly.

---

# 7. SSH Service Verification

### Command

```bash
systemctl status ssh
```

### Purpose

Verify SSH service availability.

### Observation

Service:

```text
OpenSSH Server
```

Status:

```text
Active (running)
```

### Result

SSH service is operational and ready for remote access.

---

# 8. CPU Architecture Verification

### Command

```bash
uname -m
```

### Output

```text
x86_64
```

### Explanation

Indicates the system is running on a 64-bit architecture.

---

# 9. System Uptime Verification

### Command

```bash
uptime
```

### Observation

```text
Up for 32 minutes
```

Load Average:

```text
0.00
0.02
0.06
```

### Analysis

* System load is extremely low.
* No CPU pressure observed.

---

# 10. Memory Utilization Verification

### Command

```bash
free -h
```

### Observation

Total Memory:

```text
3.8 GiB
```

Used Memory:

```text
1.1 GiB
```

Available Memory:

```text
2.7 GiB
```

Swap:

```text
0 B
```

### Analysis

* Memory utilization is healthy.
* Sufficient memory available for Docker and Kubernetes labs.

---

# 11. Storage Device Inspection

### Command

```bash
lsblk
```

### Purpose

Display available block devices.

### Observation

Primary Disk:

```text
sda
50 GB
```

Root Partition:

```text
sda2
```

Mounted on:

```text
/
```

---

# 12. Filesystem Utilization Verification

### Command

```bash
df -h
```

### Observation

Root Filesystem:

```text
49 GB Total
6.7 GB Used
40 GB Available
15% Utilized
```

### Analysis

Disk utilization is healthy.

No storage capacity issues detected.

---

# 13. Network Link Status Verification

### Command

```bash
ip link
```

### Purpose

Verify network interface status.

### Observation

Interfaces:

```text
lo
enp0s3
enp0s8
docker0
```

Status:

```text
UP
```

Docker Bridge:

```text
DOWN (No Containers Running)
```

### Analysis

This is expected behavior when no containers are attached to the Docker bridge.

---

# Node Health Summary

| Component            | Status     |
| -------------------- | ---------- |
| Operating System     | Healthy    |
| Kernel               | Healthy    |
| Hostname             | Configured |
| Network Interfaces   | Healthy    |
| Routing Table        | Healthy    |
| Time Synchronization | Healthy    |
| SSH Service          | Running    |
| CPU Architecture     | x86_64     |
| Memory Usage         | Healthy    |
| Storage Devices      | Healthy    |
| Filesystem Usage     | Healthy    |
| Docker Network       | Available  |
| Overall Node Health  | PASS       |

---

# Conclusion

The Linux node inspection was completed successfully.

The node is healthy and ready for:

* Docker Containerization
* Kubernetes Administration
* Helm Deployments
* ArgoCD GitOps
* AKS Practice Labs
* Platform Engineering Activities

Date:

```text
04 June 2026
```
